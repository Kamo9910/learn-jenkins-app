pipeline {
    agent any

    stages {
       stage('Build Docker image Docker'){
        steps {
            sh'''
            docker build -t my-playwright .
            '''
        }
     }
    }

}